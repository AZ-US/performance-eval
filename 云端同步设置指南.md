# 绩效考核云端同步设置指南

目标：总经理打开链接，填完员工考评后点击“保存并提交云端”；Amy 在任意电脑打开同一个链接，点击“同步云端记录”即可看到提交结果。

> ⚠️ **风险提示**：云端提交地址 + 密钥 合起来等同于「全员薪酬与绩效数据的钥匙」。任何人拿到这两个值都能读取所有员工的完整记录。
> - 不要把带 `?cloud=` 或 `&token=` 的专属链接发到群聊、公开文档或截图分享。
> - 只通过一对一私信（如微信/邮件）发给确实需要访问的人（总经理、Agnes、Damon、Chris Xu 等）。
> - 从这个版本开始，公开仓库源码里**不再内置任何可直接用的云端地址或密钥**——单独打开公开链接 `https://az-us.github.io/performance-eval/` 不带参数时，云端同步功能是关闭的，必须通过 Amy 私下发送的专属链接，或手动在“Amy管理”里填入地址和密钥才能使用。

## 1. 创建 Google Sheet

1. 打开 Google Drive，新建一个 Google Sheet。
2. 文件名建议：`绩效考核提交记录库`。
3. 打开菜单：`扩展程序` -> `Apps Script`。

## 2. 粘贴后端代码

1. 删除 Apps Script 默认代码。
2. 粘贴本项目的 [google-apps-script-backend.js](/Users/amyzhou/绩效考核/google-apps-script-backend.js) 全部内容。
3. 点击保存。

## 3. 配置共享密钥（SHARED_TOKEN）

密钥不写进代码，单独存放在 Apps Script 项目的“脚本属性”里，这样即使 `google-apps-script-backend.js` 提交到公开仓库，密钥本身也不会泄露。

1. 在 Apps Script 编辑器左侧点击 `项目设置`（齿轮图标）。
2. 找到 `脚本属性`，点击 `添加脚本属性`。
3. 属性名填 `SHARED_TOKEN`，值填一串自己生成的随机字符串（比如用密码生成器生成一个20位以上的随机串），保存。
4. 记下这串密钥，后面要填到绩效考核页面的“云端密钥”里，也会作为专属链接的 `token=` 参数。

## 4. 发布 Web App

1. 点击 `部署` -> `新建部署`。
2. 类型选择：`Web 应用`。
3. `执行身份` 选择：`我`。
4. `谁可以访问` 必须选择：`知道链接的任何人`。
5. 点击部署，并授权。
6. 复制生成的 Web App URL，格式通常类似：

```text
https://script.google.com/macros/s/xxxxxxxxxxxx/exec
```

如果打开 `/exec` 链接时跳到 Google 登录页，说明访问权限还没有设成“知道链接的任何人”。这种情况下总经理提交不会成功，需要回到 Apps Script 的部署设置里修改访问权限。

（这一步的“知道链接的任何人”是 Apps Script 部署机制的要求，不代表数据本身不设防——真正挡住陌生访问的是第3步配置的 `SHARED_TOKEN`，缺了它请求会被拒绝。）

## 5. 生成专用链接

如果新部署了 Apps Script、或者第一次配置、或者要更换密钥，按下面步骤生成新的专用链接。

1. 打开绩效考核页面并加上 `?admin=1`：

```text
https://az-us.github.io/performance-eval/?admin=1
```

2. 把 Web App URL 粘贴到“云端提交地址”，点击“保存地址”。
3. 把第3步生成的密钥粘贴到“云端密钥”，点击“保存密钥”。
4. 页面顶部“Amy管理”里的“复制总经理链接/复制Agnes财务链接/复制Chris Xu负责人链接/复制OKR专项链接”等按钮生成的链接会自动带上 `?cloud=` 和 `&token=`。
5. 按上面的风险提示，一对一私下发给对应的人。

## 5. 日常使用

总经理：

1. 打开公开链接或专用链接。
2. 填写员工姓名、月度奖金、必要备注。
3. 每完成一个员工，点击“保存并提交云端”。

Amy：

1. 打开同一个公开链接或专用链接。
2. 点击“同步云端记录”。
3. 在“员工记录库”里查看员工横向比较和个人纵向追踪。

## 注意

- 普通公开链接（不带 `?cloud=` 和 `&token=`）现在**打不开云端同步**，这是有意为之，避免陌生访客白拿全员数据。日常使用必须走 Amy 发的专属链接，或在“Amy管理”里手动填地址+密钥。
- 如果怀疑密钥外泄，回到 Apps Script `项目设置` -> `脚本属性`，把 `SHARED_TOKEN` 改成新值，然后重新走一遍“生成专用链接”，把新链接发给所有授权人（旧链接会立即失效）。
- 如果网络或 Google 服务异常，仍可以点击“导出提交包”作为兜底。
- 如果 Edge 或跨国网络环境持续显示“云端提交失败”，请把最新的 `google-apps-script-backend.js` 重新粘贴到 Apps Script，并用“管理部署/编辑部署”发布新版本。新版后端支持传统表单 POST，比普通跨域 `fetch` 更兼容。
